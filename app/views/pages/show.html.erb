<p id="notice"><%= notice %></p>

<h1>
  <%= @page.title %>
</h1>

<p class="description">
  <%= @page.description %>
</p>

<%= @page.main_html.html_safe %>
<%= render @page.view_partial_name %>

<%# if current_user.can? :edit, @page %>
  <%= link_to 'Edit', edit_page_path(@page) %> |
<%# end %>
<%= link_to 'Back', pages_path %>
